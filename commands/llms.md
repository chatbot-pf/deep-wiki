---
description: Generate llms.txt and llms-full.txt files for the wiki — LLM-friendly project summaries following the llms.txt specification
disable-model-invocation: true
---

# Deep Wiki: Generate llms.txt

> **Note (Claude Code only):** This command is a thin entrypoint. The full procedure lives in the `wiki-llms-txt` skill, which is the single source of truth and the portable unit that also works in GitHub Copilot CLI, Codex, Antigravity, and other Agent Skills runtimes.

Invoke the **`wiki-llms-txt`** skill to generate `llms.txt` and `llms-full.txt` for this repository's wiki — follow its source-repository resolution, context gathering, spec-compliant formats, output file layout, validation checklist, and generation report exactly.

$ARGUMENTS
