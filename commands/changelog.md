---
description: Generate a structured changelog from recent git commits, categorized by change type
disable-model-invocation: true
---

# Deep Wiki: Changelog Generation

> **Note (Claude Code only):** This command is a thin entrypoint. The full procedure lives in the `wiki-changelog` skill, which is the single source of truth and the portable unit that also works in GitHub Copilot CLI, Codex, Antigravity, and other Agent Skills runtimes.

Invoke the **`wiki-changelog`** skill to generate a structured changelog from this repository's git history — follow its source-repository resolution, category classification, and output format exactly.

$ARGUMENTS
